# CLAUDE.md

@AGENTS.md

`AGENTS.md` is the single source of truth for this project: stack, hard
constraints, schema, screens, build order, and the handoff protocol between
tools. Architecture decisions are in `docs/adr/`. Live progress is in
`docs/STATUS.md`. Read `docs/STATUS.md` at the start of every session.
